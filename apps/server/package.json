{
  "name": "@hotroom/server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "dev": "echo 'no dev yet'",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hotroom/shared": "workspace:*"
  }
}
